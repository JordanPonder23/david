import { SearchfuzzyDirective } from './searchfuzzy.directive';

describe('SearchfuzzyDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchfuzzyDirective();
    expect(directive).toBeTruthy();
  });
});
