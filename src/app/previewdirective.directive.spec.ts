import { PreviewdirectiveDirective } from './previewdirective.directive';

describe('PreviewdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviewdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
